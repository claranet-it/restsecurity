{
  "name": "restsecurity",
  "dependencies": {
    "@fastify/auth": "^5.0.2",
    "@types/node": "^22.10.7",
    "fastify": "^5.2.1",
    "fastify-plugin": "^5.0.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "bstart": "rimraf dist && tsc --build && node dist/app.js"
  }
}
